# Deprecated public prototype

This repository is not an active runtime and must not receive private inputs, business configuration, identifiers, secrets, or results.

Active public computation must use a neutral shared compute plane with opaque encrypted request and response capsules. Human-readable workload meaning belongs only in private authority.
